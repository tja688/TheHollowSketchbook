namespace Game.Core
{
    public enum CombatSide
    {
        None,
        Player,
        Enemy
    }

    public enum DamageType
    {
        Attack,
        HpLoss
    }

    public enum PowerType
    {
        Buff,
        Debuff
    }
}
